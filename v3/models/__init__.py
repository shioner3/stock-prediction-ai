"""Phase V3-2: Baseline ML models (spec section 4-7) - LightGBM Regression
(Model A), Binary Classification (Model B), and Quantile Regression
(Model C). Every hyperparameter is fixed in `v3/models/config.py` BEFORE
any training run and never tuned in this Phase, regardless of how the
resulting metrics look (spec section 8/24: this Phase verifies the
pipeline WORKS, it does not search for a good model).
"""
